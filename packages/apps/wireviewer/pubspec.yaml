name: wireviewer
description: The Wire character viewer app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  shared:
    path: ../shared
  wire_viewer_configuration:
    path: ../../data/configuration/wire_viewer_configuration

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true